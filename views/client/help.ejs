<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <%- include("../includes/bootstrap.ejs") %>
    <link rel="stylesheet" href="/css/help.css" />
  </head>
  <body>
    <%- include("../includes/navbar.ejs") %> <% if (links.length === 0) { %>
    <p>Nothing found</p>
    <% } else { %> <% for (link in links) { %>
    <p><%=link%></p>
    <% } %> <% } %>
  </body>
</html>
